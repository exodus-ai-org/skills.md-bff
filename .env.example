# Shared secret that callers (e.g. the exodus Electron app) must send as the
# `x-bff-auth` header when hitting /api/v1/*. Leave unset to disable the check
# (not recommended once deployed).
BFF_SHARED_SECRET=
